service: sls-22-feb
frameworkVersion: 3

provider:
  name: aws
  runtime: nodejs18.x

functions:
  api:
    handler: index.handler 
    events:
      -httpAPI:
        path: /
        method: get

plugins:
  - serverless-offline